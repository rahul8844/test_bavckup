import React from 'react';
import '../styles.scss';

export default class Dial extends React.Component{
    render(){
        return(
            <div id="dial" key="dial" className="dial"/>
        );
    }
}